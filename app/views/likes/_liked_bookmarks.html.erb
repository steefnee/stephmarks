<div>
  <ul>
    <% current_user.likes.map(&:bookmark).each do |bookmark| %>
      <%= link_to image_tag(bookmark.embedly, :height => 200, :width => 200), bookmark.url %>
    <% end %>

  </ul>
</div>
